# Story 2.3: Short Links & Redirect Engine

---

## Status

**Done**

---

## Story Statement

As a member of the internal team, I want each UTM link to have a short shareable URL, so that I can use them in channels with character limits (WhatsApp, bio, SMS).

---

## Acceptance Criteria

1. Ao criar um link UTM, um `short_slug` e gerado automaticamente (6 chars alfanumerico) — ja implementado na Story 2.1
2. Rota `/r/[slug]` implementada — ao acessar, faz redirect 302 para a `full_url` do link correspondente
3. Antes do redirect, registra o clique na tabela `tracking_events` com: tracking_id (gerado ou lido de cookie), timestamp, UTM params do link, referrer, user_agent, ip_hash
4. Short URL exibida na tabela de links e no formulario pos-criacao — ja implementado na Story 2.2
5. Botao "Copiar Short URL" — ja implementado na Story 2.2
6. Se slug nao encontrado, retorna pagina 404 amigavel
7. Redirect deve ser rapido (<200ms) — logica minima antes do 302
8. Cookie `_ptk_id` (tracking_id) setado no browser do visitante com expiracao de 180 dias

---

## Tasks

### Task 1: Create redirect route

- [x] 1.1 Criar `src/app/r/[slug]/route.ts` — GET handler
- [x] 1.2 Lookup link por `short_slug` na tabela `utm_links` usando service role client (bypass RLS para performance)
- [x] 1.3 Se nao encontrado, redirecionar para pagina 404
- [x] 1.4 Se encontrado, registrar clique e fazer redirect 302

### Task 2: Click tracking before redirect

- [x] 2.1 Gerar ou ler `tracking_id` do cookie `_ptk_id`
- [x] 2.2 Se cookie nao existe, gerar UUID v4 e setar cookie `_ptk_id` com max-age 180 dias
- [x] 2.3 Inserir evento na tabela `tracking_events` com: project_id, link_id, tracking_id, event_type='click', timestamp, utm params do link, page_url, referrer, user_agent, ip_hash
- [x] 2.4 Hash do IP com salt usando `hashIp()` de `src/lib/utils/hash.ts`
- [x] 2.5 Inserir evento de forma async (nao bloquear o redirect)

### Task 3: 404 page for invalid slugs

- [x] 3.1 Criar pagina 404 amigavel (inline HTML no route handler)
- [x] 3.2 Mensagem clara: "Link nao encontrado"

### Task 4: Tests

- [x] 4.1 Testes para redirect route (slug found, slug not found)
- [x] 4.2 Testes para tracking_id cookie generation

### Task 5: Final validation

- [x] 5.1 lint, typecheck, test, build — ALL PASS

---

## Dev Agent Record

### File List
- `src/app/r/[slug]/route.ts` — Redirect GET handler with click tracking
- `src/app/r/redirect.test.ts` — Tests for redirect engine logic
- `docs/stories/2.3.short-links-redirect-engine.story.md` — This story

### Completion Notes
- Route handler uses `createServiceRoleClient()` to bypass RLS for fast lookup
- `after()` from Next.js used for async tracking event insert (doesn't block redirect)
- 404 page rendered as inline HTML from route handler (not-found.tsx can't coexist with route.ts)
- Cookie `_ptk_id` set with 180 days max-age, httpOnly, sameSite: lax
- IP hashed using `hashIp()` with configurable `IP_HASH_SALT` env var

### Change Log
| Date | Change | Author |
|------|--------|--------|
| 2026-03-10 | Implemented redirect route, click tracking, 404, tests | @dev |
