# Story 1.2: Database Schema & Supabase Setup

---

## Status

**Ready for Review**

---

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["lint", "typecheck", "build", "test"]
```

---

## Story

**As a** desenvolvedor da equipe,
**I want** o schema do banco de dados criado no Supabase com as tabelas core,
**so that** o sistema tenha persistencia real desde o inicio.

---

## Acceptance Criteria

1. Projeto Supabase criado e conectado ao Next.js via variaveis de ambiente
2. Tabela `projects` criada: id, name, slug, description, base_url, created_at, updated_at
3. Tabela `utm_links` criada: id, project_id (FK), label, base_url, utm_source, utm_medium, utm_campaign, utm_content, utm_term, short_slug (unique), full_url (generated), created_at, updated_at
4. Tabela `tracking_events` criada: id, project_id (FK), link_id (FK nullable), tracking_id, event_type (click|conversion), timestamp, utm_source, utm_medium, utm_campaign, utm_content, utm_term, page_url, referrer, user_agent, ip_hash
5. Tabela `project_pages` criada: id, project_id (FK projects), name, url, is_default (boolean), created_at
6. Tabela `profiles` criada: id (FK auth.users), email, full_name, role, created_at
7. RLS policies habilitadas em todas as tabelas — apenas usuarios autenticados podem ler/escrever
8. Projeto-padrao seed criado: "Potencia Educacao — Geral"
9. Supabase client configurado em `src/lib/supabase/client.ts` e `server.ts`

---

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.
> To enable, set `coderabbit_integration.enabled: true` in core-config.yaml

---

## Tasks / Subtasks

- [x] **Task 1: Instalar dependencias Supabase** (AC: 1, 9)
  - [x] 1.1 Instalar `@supabase/supabase-js` e `@supabase/ssr`
  - [x] 1.2 Verificar compatibilidade com Next.js 16 e React 19

- [x] **Task 2: Configurar Supabase clients** (AC: 1, 9)
  - [x] 2.1 Criar `src/lib/supabase/client.ts` — client-side com `NEXT_PUBLIC_SUPABASE_URL` e `NEXT_PUBLIC_SUPABASE_ANON_KEY`
  - [x] 2.2 Criar `src/lib/supabase/server.ts` — server-side com `SUPABASE_SERVICE_ROLE_KEY` para API routes
  - [x] 2.3 Criar `src/lib/supabase/middleware.ts` — helper para auth middleware (usado na Story 1.3)
  - [x] 2.4 Atualizar `.env.example` com todas as variaveis Supabase
  - [x] 2.5 Testes: importar clients e verificar que nao quebram typecheck

- [x] **Task 3: Criar migration — schema completo** (AC: 2, 3, 4, 5, 6)
  - [x] 3.1 Criar `supabase/migrations/001_initial_schema.sql` com DDL completo (5 tabelas)
  - [x] 3.2 Tabela `profiles`: id (UUID PK FK auth.users), email, full_name, role (CHECK admin|member), created_at
  - [x] 3.3 Tabela `projects`: id (UUID PK), name, slug (UNIQUE), description, base_url, created_at, updated_at
  - [x] 3.4 Tabela `project_pages`: id (UUID PK), project_id (FK CASCADE), name, url, is_default, created_at
  - [x] 3.5 Tabela `utm_links`: id (UUID PK), project_id (FK CASCADE), page_id (FK SET NULL), label, base_url, utm_source, utm_medium, utm_campaign, utm_content, utm_term, short_slug (UNIQUE), full_url, created_at, updated_at
  - [x] 3.6 Tabela `tracking_events`: id (UUID PK), project_id (FK CASCADE), link_id (FK SET NULL), tracking_id, event_type (CHECK), timestamp, utm_*, page_url, referrer, user_agent, ip_hash, conversion_data (JSONB), is_orphan, created_at

- [x] **Task 4: Criar indexes de performance** (AC: 2, 3, 4, 5)
  - [x] 4.1 `idx_projects_slug` em projects(slug)
  - [x] 4.2 `idx_project_pages_project` em project_pages(project_id)
  - [x] 4.3 `idx_project_pages_default` — UNIQUE parcial (project_id WHERE is_default = true)
  - [x] 4.4 `idx_utm_links_project`, `idx_utm_links_slug`, `idx_utm_links_search` em utm_links
  - [x] 4.5 7 indexes em tracking_events: project_time, project_type_time, source, campaign (parcial), tracking_id, link (parcial), conversions (parcial)

- [x] **Task 5: Criar triggers e functions** (AC: 6)
  - [x] 5.1 Function `handle_new_user()` — auto-cria profile ao signup (SECURITY DEFINER)
  - [x] 5.2 Trigger `on_auth_user_created` em auth.users AFTER INSERT
  - [x] 5.3 Function `handle_updated_at()` — atualiza updated_at automaticamente
  - [x] 5.4 Triggers `set_updated_at` em projects e utm_links BEFORE UPDATE

- [x] **Task 6: Configurar RLS policies** (AC: 7)
  - [x] 6.1 Habilitar RLS em TODAS as 5 tabelas (`ALTER TABLE ... ENABLE ROW LEVEL SECURITY`)
  - [x] 6.2 `profiles`: SELECT e UPDATE apenas para `auth.uid() = id` (proprio perfil)
  - [x] 6.3 `projects`: CRUD completo para role `authenticated` (MVP = equipe interna)
  - [x] 6.4 `project_pages`: CRUD completo para role `authenticated`
  - [x] 6.5 `utm_links`: CRUD completo para role `authenticated`
  - [x] 6.6 `tracking_events`: SELECT para `authenticated`, INSERT apenas para `service_role` (API route)

- [x] **Task 7: Criar seed data** (AC: 8)
  - [x] 7.1 Criar `supabase/seed.sql` com INSERT do projeto-padrao "Potencia Educacao — Geral" (slug: potencia-geral, base_url: https://potencia.edu.br)
  - [x] 7.2 INSERT da pagina padrao "Site Principal" (is_default: true) vinculada ao projeto

- [x] **Task 8: Gerar TypeScript types** (AC: 9)
  - [x] 8.1 Atualizar `src/types/database.ts` com types manuais baseados no schema (Row, Insert, Update para cada tabela)
  - [x] 8.2 Exportar type `Database` com namespace `public.Tables`
  - [x] 8.3 Nota: Quando Supabase estiver rodando na VPS, substituir por `supabase gen types typescript` automatico

- [x] **Task 9: Criar repository layer (stubs)** (AC: 9)
  - [x] 9.1 Criar `src/lib/db/projects.ts` — stub com funcoes: getAll, getBySlug, create, update, remove
  - [x] 9.2 Criar `src/lib/db/pages.ts` — stub com funcoes: getByProject, create, update, remove
  - [x] 9.3 Criar `src/lib/db/links.ts` — stub com funcoes: getByProject, getBySlug, create, update, remove
  - [x] 9.4 Criar `src/lib/db/tracking.ts` — stub com funcoes: insertEvent, getKPIs, getTimeline, getTopSources
  - [x] 9.5 Criar `src/lib/db/profiles.ts` — stub com funcoes: getById, update

- [x] **Task 10: Criar funcoes utilitarias** (AC: 4)
  - [x] 10.1 Criar `src/lib/utils/hash.ts` — funcao `hashIp(ip, salt)` usando SHA-256 (truncado 16 chars)
  - [x] 10.2 Criar `src/lib/utils/utm.ts` — funcao `sanitizeUtmParam(value)` (max 200 chars, remove especiais)
  - [x] 10.3 Criar `src/lib/utils/api-error.ts` — funcao `formatApiError(status, message)` padronizada

- [x] **Task 11: Criar testes unitarios** (AC: 1-9)
  - [x] 11.1 Testar que SQL migration e sintaticamente valido (parse test)
  - [x] 11.2 Testar TypeScript types — criar objetos mock de cada tabela e verificar type safety
  - [x] 11.3 Testar funcoes utilitarias: `hashIp()`, `sanitizeUtmParam()`

- [x] **Task 12: Validacao final** (AC: 1-9)
  - [x] 12.1 `npm run lint` — sem erros
  - [x] 12.2 `npm run typecheck` — sem erros
  - [x] 12.3 `npm run build` — build com sucesso
  - [x] 12.4 `npm run test` — todos os testes passando
  - [x] 12.5 Verificar que `.env.example` documenta TODAS as variaveis necessarias

---

## Dev Notes

### Previous Story Insights [Source: Story 1.1 Completion Notes]

- ESLint 9 usa flat config (`eslint.config.mjs`), NAO `.eslintrc.json`
- globals.css esta em `src/app/globals.css` (shadcn default), NAO `src/styles/`
- shadcn/ui v4 (style: base-nova) com oklch CSS variables
- next-themes para dark mode (classe `dark` no `<html>`)
- Vitest 4.0.18 com jsdom configurado e funcionando
- Deploy na Vercel operacional em `potencia-tracking.vercel.app`

### Supabase Setup — CRITICAL [Source: architecture.md#2.2, #13.1]

**Supabase e SELF-HOSTED na VPS WeCoded (mesmo VPS do Mautic em mautic.hmnews.com.br).**

Isso significa:
- NAO usar `supabase init` local para desenvolvimento com CLI — as migrations sao scripts SQL manuais
- A conexao sera via reverse proxy: `https://supabase-api.hmnews.com.br`
- O Supabase NAO esta configurado na VPS ainda — esta story prepara os ARTEFATOS (migrations, clients, types) para quando estiver
- Por enquanto, o codigo deve compilar e os testes devem passar SEM uma instancia Supabase ativa
- Os clients Supabase devem ser configurados mas NAO serao testados contra um backend real nesta story

### Database Schema [Source: architecture.md#8.1]

5 tabelas no schema `public`:

| Tabela | Colunas | FK | Indexes |
|--------|---------|------|---------|
| `profiles` | id, email, full_name, role, created_at | auth.users(id) CASCADE | PK |
| `projects` | id, name, slug, description, base_url, created_at, updated_at | — | slug (UNIQUE) |
| `project_pages` | id, project_id, name, url, is_default, created_at | projects(id) CASCADE | project_id, default parcial |
| `utm_links` | id, project_id, page_id, label, base_url, utm_*, short_slug, full_url, created_at, updated_at | projects(id) CASCADE, project_pages(id) SET NULL | project_id, slug, search composto |
| `tracking_events` | id, project_id, link_id, tracking_id, event_type, timestamp, utm_*, page_url, referrer, user_agent, ip_hash, conversion_data, is_orphan, created_at | projects(id) CASCADE, utm_links(id) SET NULL | 7 indexes especializados |

### SQL DDL Completo [Source: architecture.md#8.1]

**profiles:**
```sql
CREATE TABLE public.profiles (
  id          UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
  email       TEXT NOT NULL,
  full_name   TEXT NOT NULL DEFAULT '',
  role        TEXT NOT NULL DEFAULT 'member' CHECK (role IN ('admin', 'member')),
  created_at  TIMESTAMPTZ NOT NULL DEFAULT now()
);
```

**projects:**
```sql
CREATE TABLE public.projects (
  id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name        TEXT NOT NULL,
  slug        TEXT NOT NULL UNIQUE,
  description TEXT,
  base_url    TEXT NOT NULL,
  created_at  TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at  TIMESTAMPTZ NOT NULL DEFAULT now()
);
CREATE INDEX idx_projects_slug ON public.projects(slug);
```

**project_pages:**
```sql
CREATE TABLE public.project_pages (
  id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  project_id  UUID NOT NULL REFERENCES public.projects(id) ON DELETE CASCADE,
  name        TEXT NOT NULL,
  url         TEXT NOT NULL,
  is_default  BOOLEAN NOT NULL DEFAULT false,
  created_at  TIMESTAMPTZ NOT NULL DEFAULT now()
);
CREATE INDEX idx_project_pages_project ON public.project_pages(project_id);
CREATE UNIQUE INDEX idx_project_pages_default ON public.project_pages(project_id) WHERE is_default = true;
```

**utm_links:**
```sql
CREATE TABLE public.utm_links (
  id           UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  project_id   UUID NOT NULL REFERENCES public.projects(id) ON DELETE CASCADE,
  page_id      UUID REFERENCES public.project_pages(id) ON DELETE SET NULL,
  label        TEXT NOT NULL,
  base_url     TEXT NOT NULL,
  utm_source   TEXT NOT NULL,
  utm_medium   TEXT NOT NULL DEFAULT '',
  utm_campaign TEXT NOT NULL DEFAULT '',
  utm_content  TEXT,
  utm_term     TEXT,
  short_slug   TEXT NOT NULL UNIQUE,
  full_url     TEXT NOT NULL,
  created_at   TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at   TIMESTAMPTZ NOT NULL DEFAULT now()
);
CREATE INDEX idx_utm_links_project ON public.utm_links(project_id);
CREATE INDEX idx_utm_links_slug ON public.utm_links(short_slug);
CREATE INDEX idx_utm_links_search ON public.utm_links(project_id, label, utm_source, utm_campaign);
```

**tracking_events:**
```sql
CREATE TABLE public.tracking_events (
  id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  project_id      UUID NOT NULL REFERENCES public.projects(id) ON DELETE CASCADE,
  link_id         UUID REFERENCES public.utm_links(id) ON DELETE SET NULL,
  tracking_id     TEXT NOT NULL,
  event_type      TEXT NOT NULL CHECK (event_type IN ('pageview', 'click', 'conversion')),
  timestamp       TIMESTAMPTZ NOT NULL DEFAULT now(),
  utm_source      TEXT NOT NULL DEFAULT '',
  utm_medium      TEXT NOT NULL DEFAULT '',
  utm_campaign    TEXT NOT NULL DEFAULT '',
  utm_content     TEXT,
  utm_term        TEXT,
  page_url        TEXT NOT NULL DEFAULT '',
  referrer        TEXT,
  user_agent      TEXT NOT NULL DEFAULT '',
  ip_hash         TEXT NOT NULL DEFAULT '',
  conversion_data JSONB,
  is_orphan       BOOLEAN NOT NULL DEFAULT false,
  created_at      TIMESTAMPTZ NOT NULL DEFAULT now()
);
CREATE INDEX idx_tracking_project_time ON public.tracking_events(project_id, timestamp DESC);
CREATE INDEX idx_tracking_project_type_time ON public.tracking_events(project_id, event_type, timestamp DESC);
CREATE INDEX idx_tracking_source ON public.tracking_events(project_id, utm_source, timestamp DESC);
CREATE INDEX idx_tracking_campaign ON public.tracking_events(project_id, utm_campaign, timestamp DESC) WHERE utm_campaign != '';
CREATE INDEX idx_tracking_tracking_id ON public.tracking_events(tracking_id);
CREATE INDEX idx_tracking_link ON public.tracking_events(link_id) WHERE link_id IS NOT NULL;
CREATE INDEX idx_tracking_conversions ON public.tracking_events(project_id, timestamp DESC) WHERE event_type = 'conversion';
```

### Triggers & Functions [Source: architecture.md#8.1, #8.4]

```sql
-- Auto-create profile on signup
CREATE OR REPLACE FUNCTION public.handle_new_user()
RETURNS TRIGGER AS $$
BEGIN
  INSERT INTO public.profiles (id, email, full_name)
  VALUES (NEW.id, NEW.email, COALESCE(NEW.raw_user_meta_data->>'full_name', ''));
  RETURN NEW;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;

CREATE TRIGGER on_auth_user_created
  AFTER INSERT ON auth.users
  FOR EACH ROW EXECUTE FUNCTION public.handle_new_user();

-- Auto-update updated_at
CREATE OR REPLACE FUNCTION public.handle_updated_at()
RETURNS TRIGGER AS $$
BEGIN
  NEW.updated_at = now();
  RETURN NEW;
END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER set_updated_at BEFORE UPDATE ON public.projects
  FOR EACH ROW EXECUTE FUNCTION public.handle_updated_at();
CREATE TRIGGER set_updated_at BEFORE UPDATE ON public.utm_links
  FOR EACH ROW EXECUTE FUNCTION public.handle_updated_at();
```

### RLS Policies [Source: architecture.md#8.2]

- `profiles`: SELECT/UPDATE apenas `auth.uid() = id`
- `projects`, `project_pages`, `utm_links`: CRUD completo para `authenticated` (MVP equipe interna)
- `tracking_events`: SELECT para `authenticated`, INSERT apenas para `service_role`

### Seed Data [Source: architecture.md#8.3]

```sql
INSERT INTO public.projects (name, slug, description, base_url)
VALUES ('Potencia Educacao — Geral', 'potencia-geral', 'Projeto padrao para tracking geral da Potencia Educacao', 'https://potencia.edu.br');

INSERT INTO public.project_pages (project_id, name, url, is_default)
SELECT id, 'Site Principal', 'https://potencia.edu.br', true
FROM public.projects WHERE slug = 'potencia-geral';
```

### Supabase Client Pattern [Source: architecture.md#16.1]

- `client.ts` — usa `createBrowserClient` com `NEXT_PUBLIC_*` keys
- `server.ts` — usa `createServerClient` com `SUPABASE_SERVICE_ROLE_KEY` para inserts de tracking
- **REGRA:** `SUPABASE_SERVICE_ROLE_KEY` NUNCA em client components. Apenas API routes/RSC
- **Token Storage:** httpOnly cookies via `@supabase/ssr`

### Environment Variables [Source: architecture.md#13.3]

```bash
NEXT_PUBLIC_SUPABASE_URL=https://supabase-api.hmnews.com.br
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbG...
SUPABASE_SERVICE_ROLE_KEY=eyJhbG...
IP_HASH_SALT=random-string-change-me
```

### Utility Functions [Source: architecture.md#14.2, #10.6]

**hashIp:**
```typescript
import crypto from 'crypto';
export function hashIp(ip: string, salt: string): string {
  return crypto.createHash('sha256').update(ip + salt).digest('hex').substring(0, 16);
}
```

**sanitizeUtmParam:**
```typescript
export function sanitizeUtmParam(value: string): string {
  return value.substring(0, 200).replace(/[^a-zA-Z0-9_\-]/g, '');
}
```

### File Locations [Source: architecture.md#11]

```
src/lib/supabase/client.ts       # Browser Supabase client
src/lib/supabase/server.ts       # Server Supabase client (service_role)
src/lib/supabase/middleware.ts    # Auth middleware helper (prep para Story 1.3)
src/lib/db/projects.ts           # Repository: projects
src/lib/db/pages.ts              # Repository: project_pages
src/lib/db/links.ts              # Repository: utm_links
src/lib/db/tracking.ts           # Repository: tracking_events
src/lib/db/profiles.ts           # Repository: profiles
src/lib/utils/hash.ts            # IP hashing (SHA-256)
src/lib/utils/utm.ts             # UTM param sanitization
src/lib/utils/api-error.ts       # API error formatting
src/types/database.ts            # Generated TypeScript types
supabase/migrations/001_initial_schema.sql  # DDL + indexes + triggers + RLS
supabase/seed.sql                # Default project + page
```

### Testing Strategy [Source: architecture.md#15]

- Testes unitarios para funcoes utilitarias (hashIp, sanitizeUtmParam)
- Testes de type safety — criar objetos mock com os types gerados
- Testes de parse SQL — verificar que a migration e sintaticamente valida
- NAO testar contra Supabase real nesta story (backend nao configurado)

### Coding Standards [Source: architecture.md#16]

- Absolute imports com `@/` (ex: `@/lib/supabase/client`)
- Server Components por padrao, `'use client'` apenas quando necessario
- Files kebab-case, Components PascalCase, Hooks use-prefix
- TypeScript strict, sem `any`, usar `unknown` + type guards

---

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-10 | 0.1 | Story draft created | River (SM) |
| 2026-03-10 | 0.2 | PO validation GO (9/10) — reordenado Task 10/11 (utilities antes de testes) | Pax (PO) |
| 2026-03-10 | 0.3 | Implementation complete — 12/12 tasks, 42/42 subtasks, 36 tests passing | Dex (Dev) |

---

## Dev Agent Record

### Agent Model Used
Claude Opus 4.6 (claude-opus-4-6)

### Debug Log References
- `server.ts` typecheck fix: `require('@supabase/supabase-js')` needed explicit type cast for generic `createClient<Database>` to work with `require()` pattern

### Completion Notes List
- @supabase/supabase-js@2.99.0 + @supabase/ssr@0.9.0 installed and compatible with Next.js 16 + React 19
- `.env.example` already had Supabase vars from Story 1.1 (lines 102-108) — no changes needed
- `createServiceRoleClient` uses `require()` instead of top-level import to keep server.ts usable in both server component and API route contexts
- All 36 tests passing across 5 test files
- SQL migration file consolidates all DDL, indexes, triggers, RLS in one file (as per architecture spec)

### File List
| File | Status | Description |
|------|--------|-------------|
| `src/lib/supabase/client.ts` | Created | Browser Supabase client |
| `src/lib/supabase/server.ts` | Created | Server client + service_role client |
| `src/lib/supabase/middleware.ts` | Created | Auth middleware helper (Story 1.3 prep) |
| `src/lib/db/projects.ts` | Created | Repository stubs: projects |
| `src/lib/db/pages.ts` | Created | Repository stubs: project_pages |
| `src/lib/db/links.ts` | Created | Repository stubs: utm_links |
| `src/lib/db/tracking.ts` | Created | Repository stubs: tracking_events |
| `src/lib/db/profiles.ts` | Created | Repository stubs: profiles |
| `src/lib/utils/hash.ts` | Created | hashIp (SHA-256 truncated 16 chars) |
| `src/lib/utils/utm.ts` | Modified | sanitizeUtmParam (replaced placeholder) |
| `src/lib/utils/api-error.ts` | Created | formatApiError + apiErrorResponse |
| `src/types/database.ts` | Modified | Full Database types (Row/Insert/Update per table) |
| `supabase/migrations/001_initial_schema.sql` | Created | Full DDL + indexes + triggers + RLS |
| `supabase/seed.sql` | Created | Default project + page seed |
| `src/lib/utils/hash.test.ts` | Created | 6 tests for hashIp |
| `src/lib/utils/utm.test.ts` | Modified | 8 tests for sanitizeUtmParam |
| `src/lib/utils/api-error.test.ts` | Created | 3 tests for formatApiError |
| `src/types/database.test.ts` | Created | 7 tests for type safety |
| `src/lib/db/migration.test.ts` | Created | 12 tests for SQL migration + seed validation |
| `package.json` | Modified | Added @supabase/supabase-js, @supabase/ssr |
| `package-lock.json` | Modified | Updated lockfile |

---

## QA Results
_(a ser preenchido pelo @qa)_
