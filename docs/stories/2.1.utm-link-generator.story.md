# Story 2.1: UTM Link Generator

---

## Status

**Approved**

---

## Story Statement

As a member of the internal team, I want to create UTM links through an assisted form with real-time preview, so that I generate standardized links without manual errors.

---

## Acceptance Criteria

1. Pagina `/links` com botao "Novo Link" que abre formulario de criacao (modal)
2. Seletor de projeto (dropdown) no topo do formulario — pre-selecionado com o projeto ativo, mas permite trocar
3. Seletor de pagina destino (combobox) populado com paginas cadastradas do projeto selecionado (`project_pages`), com opcao "Adicionar nova pagina" inline (nome + URL). A URL base do link e preenchida automaticamente a partir da pagina selecionada
4. Campos do formulario: Label (nome descritivo), Source, Medium, Campaign, Content (opcional), Term (opcional)
5. Source e Medium oferecem opcoes pre-definidas (Instagram, Facebook, YouTube, WhatsApp, Email, Google Ads, LinkedIn, Bio, Mautic, Site) com opcao de digitar customizado
6. Preview em tempo real da URL final montada conforme o usuario preenche os campos
7. Botao "Copiar URL" no preview com feedback visual (toast "Copiado!")
8. Validacao: Label e Source obrigatorios, pagina destino obrigatoria, sem caracteres especiais nos UTM params (auto-sanitize para kebab-case)
9. Ao salvar, link e persistido em Supabase na tabela `utm_links` vinculado ao projeto ativo
10. Apos salvar, retorna a lista de links com o novo link destacado

---

## Tasks

### Task 1: Create API routes for project pages

- [ ] 1.1 Criar `src/app/api/projects/[id]/pages/route.ts` — GET (listar paginas do projeto) e POST (criar nova pagina)
- [ ] 1.2 GET retorna paginas ordenadas por `is_default DESC, name ASC`
- [ ] 1.3 POST valida `name` e `url` obrigatorios, vincula ao `project_id`
- [ ] 1.4 Autenticacao via `supabase.auth.getUser()` em ambas as routes

### Task 2: Create API route for UTM link creation

- [ ] 2.1 Criar `src/app/api/links/route.ts` — GET (listar links do projeto) e POST (criar link)
- [ ] 2.2 GET aceita query param `project_id` para filtrar links, retorna com paginacao
- [ ] 2.3 POST valida campos obrigatorios: `project_id`, `label`, `base_url`, `utm_source`
- [ ] 2.4 POST gera `short_slug` (6 chars alfanumerico) e monta `full_url` completa com UTM params
- [ ] 2.5 POST sanitiza UTM params via `sanitizeUtmParam()` existente em `src/lib/utils/utm.ts`
- [ ] 2.6 Autenticacao via `supabase.auth.getUser()` em ambas as routes

### Task 3: Create Links page with "Novo Link" button

- [ ] 3.1 Criar `src/app/(dashboard)/links/page.tsx` — Server Component com titulo e botao "Novo Link"
- [ ] 3.2 Estado vazio quando nao ha links: mensagem "Nenhum link criado" com CTA
- [ ] 3.3 Exibir lista basica de links existentes do projeto ativo (sera expandida na Story 2.2)

### Task 4: Create UTM Link Generator form (modal)

- [ ] 4.1 Criar `src/components/links/create-link-dialog.tsx` — Dialog com formulario completo
- [ ] 4.2 Seletor de projeto (dropdown) pre-selecionado com projeto ativo do Zustand store
- [ ] 4.3 Seletor de pagina destino com opcoes do projeto selecionado + "Adicionar nova pagina" inline
- [ ] 4.4 Campos: Label, Source (combobox com presets), Medium (combobox com presets), Campaign, Content (opcional), Term (opcional)
- [ ] 4.5 Presets para Source/Medium: Instagram, Facebook, YouTube, WhatsApp, Email, Google Ads, LinkedIn, Bio, Mautic, Site
- [ ] 4.6 Auto-sanitize UTM params para kebab-case ao digitar (usar `sanitizeUtmParam`)

### Task 5: Real-time URL preview

- [ ] 5.1 Criar `src/components/links/url-preview.tsx` — componente que monta e exibe a URL final em tempo real
- [ ] 5.2 URL montada a partir de: base_url + `?utm_source=...&utm_medium=...&utm_campaign=...` + opcionais
- [ ] 5.3 Botao "Copiar URL" usa `navigator.clipboard.writeText()` com toast "Copiado!" via Sonner
- [ ] 5.4 Preview atualiza conforme o usuario digita (onChange dos campos)

### Task 6: Form submission and link persistence

- [ ] 6.1 Submit chama `POST /api/links` com payload completo
- [ ] 6.2 Loading state no botao durante submit
- [ ] 6.3 Erro exibido via toast (Sonner)
- [ ] 6.4 Sucesso fecha o dialog e atualiza a lista de links

### Task 7: Create tests

- [ ] 7.1 Testes para API de pages (GET/POST validation)
- [ ] 7.2 Testes para API de links (required fields, slug generation, UTM sanitize)
- [ ] 7.3 Testes para URL preview (URL assembly)
- [ ] 7.4 Testes para create-link-dialog (form rendering, validation)

### Task 8: Final validation

- [ ] 8.1 `npm run lint` — PASS
- [ ] 8.2 `npm run typecheck` — PASS
- [ ] 8.3 `npx vitest run` — ALL PASS
- [ ] 8.4 `npm run build` — PASS

---

## Key Technical Details

**Database Tables Used:**
- `project_pages` — paginas destino por projeto
- `utm_links` — links UTM com short_slug

**Existing Utilities:**
- `src/lib/utils/utm.ts` — `sanitizeUtmParam()` ja implementado
- `src/lib/utils/slug.ts` — `generateSlug()` disponivel
- `src/lib/utils/hash.ts` — `generateShortHash()` disponivel para short_slug

**Component Tree:**
```
/links (page)
  → CreateLinkDialog (modal)
    → Project Selector (dropdown)
    → Page Selector (combobox + inline create)
    → UTM Form Fields (Label, Source, Medium, Campaign, Content, Term)
    → UrlPreview (real-time URL assembly + copy button)
```

**Presets for Source/Medium:**
```typescript
const SOURCE_PRESETS = ['instagram', 'facebook', 'youtube', 'whatsapp', 'email', 'google-ads', 'linkedin', 'bio', 'mautic', 'site'];
const MEDIUM_PRESETS = ['social', 'cpc', 'email', 'banner', 'referral', 'organic', 'direct', 'video', 'affiliate'];
```

**Short Slug Generation:**
- 6 characters alphanumeric (a-zA-Z0-9)
- Use `generateShortHash()` from `src/lib/utils/hash.ts` or crypto randomBytes

---

## Dev Agent Record

### Completion Notes
_To be filled by @dev_

### Debug Log
_To be filled by @dev_

### Change Log
| Date | Change | Author |
|------|--------|--------|

---

## File List
_To be filled during implementation_
